name: umpar_magang_dan_pkl
description: "Aplikasi Manajemen Magang dan PKL - Universitas Muhammadiyah Parepare"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  
  # Material Design
  cupertino_icons: ^1.0.8
  
  # State Management
  provider: ^6.1.1
  
  # Networking
  http: ^1.2.0
  
  # Storage
  shared_preferences: ^2.2.2
  
  # UI Components
  google_fonts: ^6.1.0
  flutter_svg: ^2.0.9
  cached_network_image: ^3.3.1
  
  # Utils
  intl: ^0.19.0
  image_picker: ^1.0.7
  file_picker: ^6.1.1
  path_provider: ^2.1.2
  share_plus: ^7.2.1
  
  # PDF & Printing
  pdf: ^3.10.7
  printing: ^5.12.0
  
  # Loading & Animation
  shimmer: ^3.0.0
  flutter_animate: ^4.3.0
  
  # Empty States
  lottie: ^3.0.0
  
  # Pull to Refresh
  pull_to_refresh: ^2.0.0
  
  # GPS & Location
  geolocator: ^10.1.0
  permission_handler: ^11.0.1
  fl_chart: ^1.1.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
    - assets/lottie/
